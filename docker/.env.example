# Forgemaster Docker Environment Variables
# Copy this file to .env and set your values:
#   cp .env.example .env

# =============================================================================
# REQUIRED VARIABLES
# =============================================================================

# PostgreSQL password for the forgemaster database user
# Generate a strong password: openssl rand -base64 32
POSTGRES_PASSWORD=change_me_to_a_secure_password

# =============================================================================
# OPTIONAL VARIABLES (defaults shown)
# =============================================================================

# Docker socket path (for rootless Docker, use $XDG_RUNTIME_DIR/docker.sock)
# DOCKER_SOCK=/var/run/docker.sock

# Override log level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO

# Override log format (json, console)
# LOG_FORMAT=json
